---
layout: default.njk
title_word: Authors
title: All Authors
---

<h1 class="page-title">{{title}}</h1>

<ol class="authorlist list-unstyled">
  {% for authorName, authorInfo in collections.authors  %}
  {% set authorCount = authorInfo.posts.length %}
    <li class="author">
      <a href="/authors/{{authorName | lower}}">{{ authorName }}</a>
      <span class="tag-count">× {{authorCount}}</span>
      <div class="author-description">{{ authorInfo.summary }}</div>
    </li>
  {% endfor %}
</ol>